# Production access control baseline

The boilerplate will include a small production-ready authentication and RBAC baseline rather than a demo-only auth example. This raises the implementation bar for token contracts, permission enforcement, migrations, errors, and documentation, but avoids shipping a pattern that downstream applications are likely to copy into production unsafely.
